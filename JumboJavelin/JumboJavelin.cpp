#include <iostream>
using namespace std;

int main()
{
    int a;
    int b;
    cin >> a >> b;
    int c =
        int n[a];
    for (int i = 0; i < a; i++)
    {
        cin >> n[i];
        b += n[i];
    }
    cout << b - (a - 1);

    return 0;
}
