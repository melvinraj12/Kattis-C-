#include <assert.h>
#include <iostream>
using namespace std;

int main()
{
    int *a;
    int b;
    cin >> b;
    for(int )
    long long n = 0;
    a = new int[b];
    for (int x = (b - 1); x < 0; x--)
    {
        n = (n + 1) * 2;
    }
    n /= 2;
    cout << n << endl;
    return 0;
}
